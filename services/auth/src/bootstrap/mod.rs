use crate::infrastructure::in_memory_user_repository::InMemoryUserRepository;

pub fn run(_repository: InMemoryUserRepository) {
    // Bootstrap será evoluído depois
    println!("Auth bootstrap initialized");
}
